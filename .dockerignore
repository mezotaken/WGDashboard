.git
.github
*.md
tests/
docs/
src/db
src/wg-dashboard.ini

# Needed by the frontend-builder stage in docker/Dockerfile.
**/node_modules

# Rebuilt by that stage; keep the committed copy out of the build context.
src/static/dist
